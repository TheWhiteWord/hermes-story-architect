# D4 — Update Fixture Files

**Goal:** Regenerate `tests/fixtures/save-the-children/.story/index.yaml` with all fields (including structural/dramatic). Delete `structure-index.yaml` from fixture.

## Why

The fixture's `index.yaml` was generated by the old split logic — it's missing structural fields on project and dramatic metadata on scenes. After Phase A, `generate_index()` produces the complete dict. The fixture must reflect the new output.

## Current State

- `tests/fixtures/save-the-children/.story/index.yaml`: navigation-only (project missing `spine`, `controlling_idea`, `value`, `value_at_open`, `value_at_close`, `inciting_incident_scene_id`, `story_climax_scene_id`, `structure_type`; scenes missing `value`, `value_open`, `value_close`, `conflict_levels`, `dramatic_role`, `is_*`)
- `tests/fixtures/save-the-children/.story/structure-index.yaml`: exists, must be deleted
- `project.md` fixture: missing structural fields (`spine`, `controlling_idea`, `value`, `value_at_open`, `value_at_close`, `inciting_incident_scene_id`, `story_climax_scene_id`, `structure_type`) — currently only has: `name`, `genre`, `setting`, `logline`, `status`

## Changes

1. **Add structural fields to `tests/fixtures/save-the-children/project.md`** so `generate_index()` populates them:
   ```yaml
   spine: "A young person discovers the truth about their world and chooses to escape"
   controlling_idea: "Freedom requires sacrifice"
   value: Trust
   value_at_open: positive
   value_at_close: negative
   inciting_incident_scene_id: central-room-day
   story_climax_scene_id: central-room-night
   structure_type: Classical
   ```
   (These match the existing `structure-index.yaml` values.)

2. **Regenerate `index.yaml`**:
   - Delete current `index.yaml`
   - Run `refresh_index()` to regenerate it with all fields
   - Commit the new `index.yaml`

3. **Delete `tests/fixtures/save-the-children/.story/structure-index.yaml`**:
   ```bash
   rm tests/fixtures/save-the-children/.story/structure-index.yaml
   ```

## Verification Checklist

- [x] `project.md` has all structural fields
- [x] `index.yaml` regenerated with all fields (project has `spine`, `controlling_idea`, etc.; scenes have `value`, `value_open`, `value_close`, `conflict_levels`, `dramatic_role`, `is_*`)
- [x] `structure-index.yaml` deleted from fixture
- [x] `index.yaml` is valid YAML and matches what `generate_index()` produces
- [x] All remaining tests pass (67/67)

## Final Notes

Added 8 structural fields to `project.md` (spine, controlling_idea, value, value_at_open, value_at_close, inciting_incident_scene_id, story_climax_scene_id, structure_type). Regenerated `index.yaml` via `refresh_index()` — now contains full data: 3 scenes with dramatic metadata, act/sequence with values and cross-references, project with all structural fields. Deleted `structure-index.yaml` sidecar. Index went from 243 lines to 352 lines (109 lines of structural data restored).

## Notes

- Depends on Phase A (new `generate_index()` produces unified output)
- The `project.md` changes are necessary because the current fixture lacks structural fields — without them, `generate_index()` won't populate them in `index.yaml`
- The existing `structure-index.yaml` values should be used as the source for what to add to `project.md`
